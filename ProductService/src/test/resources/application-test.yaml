eureka:
  client:
    enabled: false

spring:
  kafka:
    bootstrap-servers: dummy:1234
    consumer:
      auto-offset-reset: earliest
    properties:
      enable.auto.commit: false

    listener:
      auto-startup: false

  task:
    scheduling:
      enabled: false