eureka:
  client:
    enabled: false

spring:
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      auto-offset-reset: earliest
    properties:
      enable.auto.commit: false

    listener:
      auto-startup: false

  task:
    scheduling:
      enabled: false