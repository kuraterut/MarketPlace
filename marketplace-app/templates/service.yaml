apiVersion: v1
kind: Service
metadata:
  name: {{ include "marketplace-app.fullname" . }}
  labels:
    {{- include "marketplace-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "marketplace-app.selectorLabels" . | nindent 4 }}
