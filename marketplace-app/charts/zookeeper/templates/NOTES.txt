Thank you for installing {{ .Chart.Name }}!

Your Zookeeper StatefulSet {{ include "zookeeper.fullname" . }} has been deployed with {{ .Values.replicaCount }} replicas.

You can access Zookeeper client service on port {{ .Values.service.port }} via service name: {{ include "zookeeper.serviceName" . }}.

Example to connect from inside the cluster:

  zkCli.sh -server {{ include "zookeeper.serviceName" . }}:{{ .Values.service.port }}

To check pods:

  kubectl get pods -l app.kubernetes.io/name={{ include "zookeeper.fullname" . }}

