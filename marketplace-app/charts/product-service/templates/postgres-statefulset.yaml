apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "product-service.fullname" . }}-postgres
  labels:
    app: {{ include "product-service.name" . }}-postgres
    release: {{ .Release.Name }}
spec:
  selector:
    matchLabels:
      app: {{ include "product-service.name" . }}-postgres
      release: {{ .Release.Name }}
  serviceName: {{ include "product-service.fullname" . }}-postgres
  replicas: 1
  template:
    metadata:
      labels:
        app: {{ include "product-service.name" . }}-postgres
        release: {{ .Release.Name }}
    spec:
      containers:
        - name: postgres
          image: postgres:latest
          env:
            - name: POSTGRES_DB
              value: {{ .Values.postgresql.database }}
            - name: POSTGRES_USER
              value: {{ .Values.postgresql.username }}
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "product-service.fullname" . }}-secret
                  key: postgres-password
          ports:
            - containerPort: 5432
              name: postgres
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: postgres-data
      spec:
        accessModes: [ "ReadWriteOnce" ]
        resources:
          requests:
            storage: {{ .Values.postgresql.persistence.size }}
        storageClassName: {{ .Values.postgresql.persistence.storageClass | default "standard" }}
