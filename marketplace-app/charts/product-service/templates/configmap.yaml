apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "product-service.name" . }}-config
  labels:
    {{- include "product-service.labels" . | nindent 4 }}
data:
  SPRING_PROFILES_ACTIVE: "{{ .Values.global.springProfile }}"
  JWT_SECRET: "{{ .Values.global.jwt.secret }}"
  SPRING_DATASOURCE_URL: >-
    jdbc:postgresql://{{ include "product-service.name" . }}-postgres:{{ .Values.postgresql.port }}/{{ .Values.postgresql.database }}
  SPRING_DATASOURCE_USERNAME: "{{ .Values.postgresql.username }}"
  SPRING_DATASOURCE_PASSWORD: "{{ .Values.postgresql.password }}"
  SPRING_DATA_REDIS_HOST: {{ include "product-service.name" . }}-keydb
  SPRING_DATA_REDIS_PORT: "{{ .Values.keydb.port }}"
  SPRING_KAFKA_BOOTSTRAP_SERVERS: "{{ include "product-service.kafkaBootstrap" . }}"
  SERVER_PORT: "{{ .Values.service.port}}"
  KAFKA_CONSUMER_GROUP_ID: "{{ .Values.kafka.consumerGroupId}}"

  SPRING_APPLICATION_NAME: {{ include "product-service.name" . | quote}}
  EUREKA_INSTANCE_HOSTNAME: {{ include "product-service.name" . | quote}}
  EUREKA_INSTANCE_PREFER_IP_ADDRESS: "false"