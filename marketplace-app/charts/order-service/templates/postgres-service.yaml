apiVersion: v1
kind: Service
metadata:
  name: {{ include "order-service.fullname" . }}-postgres
  labels:
    app: postgres
    release: {{ .Release.Name }}
spec:
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  clusterIP: None  # headless service для StatefulSet
  selector:
    app: postgres
    release: {{ .Release.Name }}
