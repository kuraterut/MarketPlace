apiVersion: v1
kind: Service
metadata:
  name: {{ include "order-service.name" . }}-postgres
  labels:
    app: {{ include "order-service.name" . }}-postgres
    release: {{ .Release.Name }}
spec:
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
  clusterIP: None  # headless service для StatefulSet
  selector:
    app: {{ include "order-service.name" . }}-postgres
    release: {{ .Release.Name }}
