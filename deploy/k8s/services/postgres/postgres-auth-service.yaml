apiVersion: v1
kind: Service
metadata:
  name: postgres-auth-service
  labels:
    app: postgres-auth
    role: master
spec:
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  selector:
    app: postgres-auth
    role: master
  type: ClusterIP