apiVersion: v1
kind: Service
metadata:
  name: postgres-product-service-headless
  labels:
    app: postgres-product
spec:
  clusterIP: None
  ports:
    - port: 5432
      name: postgres
    - port: 8008
      name: patroni
  selector:
    app: postgres-product