apiVersion: v1
kind: Service
metadata:
  name: postgres-product-service
  labels:
    app: postgres-product
    role: master
spec:
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  selector:
    app: postgres-product
    role: master
  type: ClusterIP